# Fishing Predictor — GitHub Pages Ready

- No service worker registration (avoids 404s on GH Pages)
- Manifest + icons included
- USGS bbox search + OSM fallback
- Tailwind + Vite React

## Local
npm install
npm run dev

## Build
npm run build
